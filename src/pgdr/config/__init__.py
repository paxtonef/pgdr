"""Governance data: safety_rules, symptom_taxonomy, questions, business_rules.

These are packaged as resources (not loaded relative to __file__) so they
work identically whether PGDR is run from source or installed as a wheel.
See config_loader.py.
"""
